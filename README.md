# FIR-Compare
Standalone FIR comparison in a local browser, with offline HTML reports and optional numerical JSON export. FIR coefficients retain their native sample rates and are never resampled. Measurement inputs must be impulse responses.
